server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
          native:
           search-locations:
             - file:./configs/
             - file:./configs/{application}/
             - file:../configs/
             - file:../configs/{application}/

  profiles:
    active: native

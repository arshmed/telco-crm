apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  labels:
    app: product-catalog-service
spec:
  type: ClusterIP
  selector:
    app: product-catalog-service
  ports:
    - name: http
      port: 9003
      targetPort: 9003
      protocol: TCP

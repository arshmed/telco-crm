apiVersion: v1
kind: Service
metadata:
  name: subscription-service
  labels:
    app: subscription-service
spec:
  type: ClusterIP
  selector:
    app: subscription-service
  ports:
    - name: http
      port: 9005
      targetPort: 9005
      protocol: TCP

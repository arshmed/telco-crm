apiVersion: v1
kind: Service
metadata:
  name: usage-service
  labels:
    app: usage-service
spec:
  type: ClusterIP
  selector:
    app: usage-service
  ports:
    - name: http
      port: 9006
      targetPort: 9006
      protocol: TCP
